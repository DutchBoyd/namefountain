<%= form_for(@domain) do |f| %>
  <% if @domain.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@domain.errors.count, "error") %> prohibited this domain from being saved:</h2>

      <ul>
      <% @domain.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :domainname %><br />
    <%= f.text_field :domainname %>
  </div>
  <div class="field">
    <%= f.label :lastknownexpiry %><br />
    <%= f.datetime_select :lastknownexpiry %>
  </div>
  <div class="field">
    <%= f.label :lastchecked %><br />
    <%= f.datetime_select :lastchecked %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
